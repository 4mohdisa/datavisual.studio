import Landing from '../components/landing/Landing';

export const metadata = {
  title: 'datavisual.studio — living dashboards & deeply-researched reports',
  description:
    'Upload a file or connect your database. Get an instantly editable dashboard, then let a council of AI models research your question on the live web.',
};

export default function LandingPage() {
  return <Landing />;
}
